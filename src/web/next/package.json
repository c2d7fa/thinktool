{
  "private": true,
  "scripts": {
    "build-static": "cp ../../static/icon.png public && cp node_modules/@thinktool/client/dist/app.css public",
    "dev": "npm run build-static && next dev",
    "build": "npm run build-static && next build && next export"
  },
  "dependencies": {
    "@thinktool/client": "^3.0.0",
    "next": "^10.0.1",
    "react": "^17.0.1",
    "react-dom": "^17.0.1"
  },
  "devDependencies": {
    "@types/node": "^14.14.6"
  }
}
