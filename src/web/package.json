{
  "name": "thinktool-web-client",
  "private": true,
  "devDependencies": {
    "parcel-bundler": "^1.12.4",
    "thinktool-client": "file:../client",
    "typescript": "^3.8.3"
  },
  "scripts": {
    "bundle": "npx parcel build -d ../../dist/static -o bundle.js -t browser main.tsx",
    "watch": "npx parcel watch -d ../../dist/static -o bundle.js -t browser main.tsx"
  }
}
