{
  "name": "thinktool-desktop-client",
  "private": true,
  "devDependencies": {
    "electron": "^8.3.0",
    "parcel-bundler": "^1.12.4",
    "typescript": "^3.8.3"
  },
  "dependencies": {
    "thinktool-client": "file:../client"
  },
  "scripts": {
    "check": "tsc --noEmit",
    "build-static": "cp ../../dist/static/* dist && cp index.html dist",
    "build": "tsc && npm run build-static",
    "start": "electron dist/start.js"
  }
}
